# Copy-paste prompt for the next AI agent

Use this prompt from the root of the extracted handoff directory:

> Read `AGENTS.md`, `README.md`, `EXPERIMENT_SCOPE.md`, `PILOT_TEST_CHECKLIST.md`, and `HANDOFF_RECEIPT.json` completely before changing anything. The default target is `current_pilot_3x4/`. First inspect the current German-English page and run the existing rebuild and tests. Then implement only the requested webpage improvement, preserve the 12 native method paths and provenance, regenerate `index.html`, rerun geometry and end-to-end checks, and report the changed files plus any effect on participant behavior, result schema or experiment scope. Do not deploy or upload participant results unless I explicitly ask and provide the destination.

For a visual-only request, add the concrete requirement after the quoted prompt. For a study-design request, explicitly say whether it concerns the current 3×4 pilot or the planned 6×2 formal design.

